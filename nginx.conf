server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # La magia para las SPAs (Evita el Error 404 al recargar)
        try_files $uri $uri/ /index.html;
    }
}